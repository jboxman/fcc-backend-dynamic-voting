import * as Types from './pollActionTypes';

/*
  - isLoading
  - any other meta status?
  - result of normalizr where the actual data will live
  - all of this will live under pollReducer key
*/

export default function pollReducer(state = {}, action) {
  return state;
}
